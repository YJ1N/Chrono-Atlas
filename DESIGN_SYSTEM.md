# DESIGN_SYSTEM.md

> **상태: 초안.** 토큰 값은 Phase 5 에서 확정한다. 지금 확정한 것은 *정책* 이다 — 정책은 Phase 2 렌더링 코드가 따라야 하므로 먼저 정해야 한다.

## 방향

Apple · Linear · Vercel · National Geographic · Google Maps.

목표는 **읽는 것이 아니라 탐색하는 경험**이다. 화면의 주인공은 타임라인이고, 나머지 UI 는 사라져야 한다.

---

## 타이포그래피

| 역할 | 서체 | 근거 |
|---|---|---|
| UI · 라벨 | **Inter Variable** | Linear/Vercel 계열 톤. `next/font` 로 셀프호스팅 |
| 시대명 · 제목 | **Instrument Serif** 또는 **Newsreader** (Phase 5 결정) | National Geographic 계열 무게감 |

### `tabular-nums` — 놓치기 쉬운 필수 항목

시간축 눈금 라벨에는 반드시 `font-variant-numeric: tabular-nums` 를 적용한다.

비례 폭 숫자를 쓰면 팬 중에 `"1,860 CE"` → `"1,870 CE"` 로 바뀔 때 라벨 폭이 미세하게 달라져 눈금 전체가 떨린다. 60fps 를 달성해도 이것 하나로 싸구려처럼 보인다.

---

## 색

### 원칙

`Category.colorToken` 은 **색상값이 아니라 토큰 이름**을 담는다. 색상 리터럴이 데이터에 들어가면 다크/라이트 테마와 접근성 대비를 한 곳에서 관리할 수 없다.

```
domains/history/categories.ts  →  colorToken: 'category.conflict'
DESIGN_SYSTEM (CSS 변수)       →  --color-category-conflict: …
```

### 요구사항

- 라이트/다크 양쪽 정의. 다크가 기본이다 — 심원한 시간을 다루는 제품의 톤에 맞고, 마크의 채도가 살아난다.
- 카테고리 색은 **최대 8개**. 그 이상은 사람이 구별하지 못한다.
- 대비: 텍스트 WCAG AA(4.5:1), 마크는 배경 대비 3:1 이상.
- 색만으로 카테고리를 구분하지 않는다 — 형태나 위치(레인)를 병행한다. 색각 이상 대응.

### 레이어별 시각 위계

| 레이어 | 처리 |
|---|---|
| 시대 리본 (`context`) | 낮은 채도, 낮은 불투명도. 배경으로 물러난다 |
| 이벤트 마크 (`primary`) | 완전 채도. `significance` 에 따라 크기 변조 |
| 선택된 아이템 | 강조 링 + 패널 연동 |

`significance` 를 **불투명도에 매핑하지 않는다.** 중요도가 낮은 항목이 흐릿하면 "렌더링이 덜 됐나" 로 읽힌다. 크기와 라벨 유무로 표현한다.

---

## 모션 정책

`prefers-reduced-motion: reduce` 를 존중한다. 뷰포트 애니메이션은 즉시 이동으로, 패널 트랜지션은 페이드로 대체한다.

| 대상 | 방식 | 금지 |
|---|---|---|
| 뷰포트 이동·줌 | 자체 rAF + `interpolateViewport`(로그 보간) | 라이브러리 사용 |
| 패널·모달·툴팁 | `motion` + `LazyMotion`/`domAnimation` | 풀 번들 import |
| 마크 진입·퇴장 | CSS transition 의 `opacity` | 마크마다 motion 컴포넌트 |
| 팬 중 | `transform: translateX()` 만 | 레이아웃 속성 변경 |

**줌은 반드시 로그 보간이어야 한다.** 선형 보간하면 줌 아웃이 처음에만 빠르고 끝에서 멈춘 것처럼 보인다. `interpolateViewport` 가 이미 이를 구현하고 있으며 테스트로 고정되어 있다.

### 지속 시간

- 뷰포트 이징: 250~400ms (거리에 따라)
- 패널: 180~220ms
- 호버: 80~120ms

---

## 레이아웃

```
┌──────────────────────────────────────────────┐
│  [검색 ⌘K]                          [필터]   │  ← 최소한의 크롬
├──────────────────────────────────────────────┤
│                                              │
│   시대 리본 (context)                        │
│   ─────────────────────────────────────      │
│   레인 1  ● ──────●        ●                 │
│   레인 2       ●      ●─────────●            │  ← 주인공
│   레인 3  ●        ●                         │
│                                              │
├──────────────────────────────────────────────┤
│  ▁▂▃▅▇▅▃▂▁▂▃▅▇  밀도 미니맵                  │  ← 전체 맥락
├──────────────────────────────────────────────┤
│  시간축 눈금 (tabular-nums)                  │
└──────────────────────────────────────────────┘
```

상세 패널은 오른쪽에서 슬라이드 인하며, **타임라인을 가리지 않고 밀어낸다**. 선택한 항목이 화면에서 사라지면 맥락을 잃는다.

---

## 접근성

- 키보드만으로 탐색 가능: 화살표(팬), `+`/`-`(줌), `Tab`(마크 순회), `Enter`(선택), `Esc`(패널 닫기)
- 마크는 `role="button"` + `aria-label` 에 제목과 시점
- 타임라인 전체에 `aria-live` 영역으로 현재 뷰포트 범위 안내
- 포커스 링은 항상 보이게 (`:focus-visible`)
