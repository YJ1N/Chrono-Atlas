# DESIGN_SYSTEM.md

> **상태: Phase 2R 기준 확정.** 이 문서의 모든 항목은 `src/app/globals.css` 및 `src/components/atlas/` 의 실제 구현과 1:1 대응한다. 문서와 코드가 갈라지면 그것은 버그다.

## 방향

Apple · Linear · Vercel · National Geographic · Google Maps.

**화면은 차트가 아니라 풍경이다.** 목표는 읽는 것이 아니라 탐험하는 경험이며, 주인공은 시간이고 UI 는 사라져야 한다.

---

## 공간 모델

```
X = 시간 (뷰포트 내 선형)
Y = 중요도 (연속) — 지형의 고도
```

레인 없음. 축 구분선 없음. 카테고리 범례 없음.

세로 팬은 별도 제스처가 아니라 **줌에 결합**되어 있다 (ADR-013). 확대하면 중요도 하한이 내려가 사소한 사건이 아래에서 떠오른다.

---

## 색

### 원칙

- `Category.colorToken` 은 색상값이 아니라 **토큰 이름**을 담는다. 색 리터럴이 데이터에 들어가면 테마와 대비를 한 곳에서 관리할 수 없다.
- **악센트는 하나뿐이다.** 여럿이면 어느 것도 강조가 되지 않는다.
- **무지개 카테고리 팔레트를 전면에 쓰지 않는다.** 그것이 이전 UI 가 대시보드로 읽힌 이유 중 하나였다.

### 토큰 — `src/app/globals.css`

| 토큰 | 역할 |
|---|---|
| `--background` `--foreground` | 기본 표면. 다크가 기본값 |
| `--surface` `--surface-raised` | 패널·수평선 배경 |
| `--border` `--muted` | 경계선·보조 텍스트 |
| `--accent` | **유일한 악센트.** 현재 시점 · 선택 상태 · 수평선의 현재 위치 |
| `--peak` | 봉우리의 기본색(뼈색). 카테고리가 아니다 |
| `--category-*` | 7개. 발광 후광과 선택 시에만 드러난다 |
| `--terrain-rgb` `--terrain-ridge` | 지형. **Canvas 가 `getComputedStyle` 로 읽는다** |

> Canvas 2D 컨텍스트는 `var(--x)` 를 이해하지 못한다. `TerrainLayer` 의 `readToken()` 이 그 다리를 놓는다. 하드코딩하면 라이트 테마에서 어긋난다.

### 배경이 시대를 말한다

`EraBackdrop` 이 뷰포트 중심에 따라 배경 색조를 로그 공간에서 보간한다.

| 시대 | 색조 |
|---|---|
| 우주 (13.8 Ga) | 보라 |
| 생명 (3.7 Ga) | 청록 |
| 문명 (1e5 이후) | 황금 |

의식적으로 읽히지는 않지만, 심원한 시간에서 문명으로 이동할 때 "장소가 바뀌었다" 는 감각을 만든다.

### 요구사항

- 라이트/다크 양쪽 정의. 다크가 기본
- 카테고리 색 최대 8개
- 색만으로 구분하지 않는다 — Y 위치(중요도)와 크기가 병행 신호다

---

## 타이포그래피

| 역할 | 크기 | 서체 |
|---|---|---|
| 콜드 오픈 숫자 | `clamp(40px, 7vw, 104px)` | Inter, `tabular-nums` |
| 커서 리드아웃 | 64px | Inter, `tabular-nums` |
| 패널 연도 | 58px | Inter, `tabular-nums` |
| 사건 제목 (패널) | 26px | **Instrument Serif** |
| 봉우리 라벨 | 12px | Inter |
| 요약·보조 | 11–14px | Inter |
| 수평선·시대 밴드 | 10–11px | Inter |

**극단적 스케일 대비가 위계를 만든다.** 이전 UI 는 전부 11px 이라 대시보드로 읽혔다.

### 서체 연결

`next/font` 로 셀프호스팅하고 `@theme inline` 에서 Tailwind 에 연결한다.

```
--font-sans:  var(--font-inter), … "Apple SD Gothic Neo", "Noto Sans KR"
--font-serif: var(--font-instrument-serif), "Apple SD Gothic Neo", …
```

> 연결이 없으면 폰트를 로드해놓고 실제로는 시스템 서체가 쓰인다. 실제로 그렇게 되어 있었고 2R-6 감사에서 잡혔다.

### `tabular-nums` — 타협 불가

모든 숫자에 `.tabular` 를 적용한다. 비례폭이면 팬 중에 `"1,860 CE"` → `"1,870 CE"` 로 바뀔 때 라벨 폭이 미세하게 달라져 전체가 떨린다. **60fps 를 달성해도 이것 하나로 싸구려처럼 보인다.**

---

## 레이아웃

```
┌──────────────────────────────────────────────────────────────────────┐
│ ChronoAtlas  역사              historical · 1 BCE  [인류사] [138억년] │ ← 크롬 최소
│                                                                      │
│              시대 밴드 (cosmic/epochal 에서만)                        │
│                                                                      │
│                                    ●산업혁명                          │
│              ●로마 제국                    ●제2차 세계대전            │ ← 봉우리 (Y=중요도)
│                        ●르네상스                                      │
│         ▁▂▄▆█▇▅▃▂▁▂▃▅▇███▆▄▂▁▂▄▆████████▇▅▃▂▁                       │ ← 능선
│    ░░▒▒▓▓████████████████████████████████████████████▓▓▒▒░░    ┊지금 │ ← 지형 + 현재 마커
│                                                                      │
│                          1,969 CE                                    │ ← 커서 리드아웃
│                          56년 전                                      │
├──────────────────────────────────────────────────────────────────────┤
│ 빅뱅   지구  생명   캄브리아  공룡멸종   인류   농업  ▮▮      현재    │ ← 로그 수평선 (52px)
└──────────────────────────────────────────────────────────────────────┘
                                              └ 바깥이 어둡다 = 현재 위치
```

선택 시 우측에서 패널(`min(420px, 92vw)`)이 슬라이드한다.

---

## 모션

`prefers-reduced-motion: reduce` 를 존중한다. 콜드 오픈은 **아예 실행하지 않고**, 전이는 즉시 완료된다.

| 대상 | 방식 | 금지 |
|---|---|---|
| 뷰포트 이동·줌 | 자체 rAF + 로그 보간 + 스프링 | 라이브러리 사용 |
| 팬 관성 | 지수 마찰, 60ms 속도 시간창 | — |
| 티어 변태 | 가중치 기반 `opacity` 크로스페이드 | 하드 스위치 |
| 패널 | CSS transition 220ms `cubic-bezier(0.32,0.72,0,1)` | — |
| 마크 위치 | `style.transform` 직접 갱신 | React 재렌더 |

### `motion` 라이브러리를 쓰지 않는다

패널 슬라이드는 CSS transition 으로 충분하고, 뷰포트 물리는 직접 적분해야 한다(ADR-002·ADR-014). 현재 `motion` 은 `package.json` 에만 있고 실제 코드에서 쓰이지 않는다 — **Phase 3 이후에도 쓰이지 않으면 제거한다.**

### 콜드 오픈

138억 년에서 인류사로 4.2초간 날아 들어간다. 숫자는 뷰포트를 구독해 따라가므로 애니메이션이 두 곳에 정의되지 않는다.

- 아무 입력이나 건너뛴다. **건너뛸 수 없는 인트로는 오만이다**
- 세션당 한 번. "봤음" 은 시작이 아니라 **끝났을 때** 기록한다 — 시작 시점에 기록하면 StrictMode 이중 실행에서 인트로가 멈춰 버린다

---

## 접근성

- 키보드: `←→` 팬, `+`/`-` 줌, `Esc` 패널 닫기
- 플롯은 `role="application"` + 한국어 `aria-label`
- 수평선은 `role="slider"` + `aria-valuemin/max/now`
- 포커스 링은 항상 보이게 (`:focus-visible`)
- 대비: 텍스트 WCAG AA(4.5:1)

**미해결:** 봉우리 개별 요소의 키보드 순회(`Tab`)와 스크린리더 대응은 아직 없다. Phase 4 항목이다.
