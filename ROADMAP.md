# ROADMAP.md

## MVP 정의

**포함**

- 138억 년 ~ 1년 연속 줌 타임라인, 60fps
- 범주형 레인 4~6개
- LOD 기반 선별 + 라벨 충돌 회피
- 적응형 시간축 눈금
- 이벤트 선택 → 상세 패널
- URL 딥링크 (뷰포트 + 선택 상태)
- 전체 밀도 미니맵
- Wikidata ETL 산출 5,000~20,000건
- 키보드 탐색, `prefers-reduced-motion`

**명시적 제외** — 범위 폭발 방지

- 지도 / GeoJSON (`location` 필드는 데이터에 존재, 렌더링만 유예)
- 관계 그래프 시각화
- 다중 도메인 전환 UI (구조는 지원, UI 는 후순위)
- 사용자 계정 / 저장 / 댓글
- 다국어
- Canvas 렌더러

---

## Phase

### ✅ Phase 0 — 기반

Next.js 16 + TS + Tailwind 스캐폴딩, git 초기화, **ESLint 엔진 경계 규칙**, 문서 6종, Claude 스킬 설치(`webapp-testing`, `skill-creator`).

경계 규칙은 실제 위반 파일로 발동을 확인했다. 발동하지 않는 규칙은 무의미하다.

### ✅ Phase 1 — 시간 커널

`TimePoint` · `TimeScale` · `ticks` · `IntervalIndex` 를 **순수 TS** 로 완성. 테스트 75개 통과.

> **이 단계를 먼저 한 이유**
> 시간 커널은 UI 가 없어 지루해 보이지만, 유일하게 나중에 못 고치는 층이다. 반대로 Phase 2 는 화려하지만 커널이 흔들리면 전부 재작업이다.

이 단계에서 발견해 해결한 것:
- 128억 년 지점에서 하루 폭 줌 시 float64 상쇄 오차 → 정밀도 하한을 **줌 한계로 승격** (ADR-006)
- 심원한 시간 확대 시 눈금 라벨 중복 → 단위를 **스텝 크기 기준**으로 선택 (ADR-011)
- d3 의존성이 실제로는 전부 불필요함을 확인 (ADR-008)

### ✅ Phase 2 — 뷰포트 & 렌더

`ViewportController` + `lod.ts` + `collision.ts` + SVG 렌더 + 입력 처리.
시드 데이터 195건(빅뱅~2022, primary 189 / context 6)으로 검증했다.

**브라우저 실측 결과** (`npm run verify:browser`, 1440×900):
p50 프레임 간격 16.7ms = **60fps**, 드롭 0%, 최악 17.7ms.

이 단계에서 발견해 해결한 것 — 모두 **브라우저 실측이 잡아냈고 단위 테스트로는
잡히지 않았다**:

- **마크를 클릭해도 선택되지 않던 버그.** `pointerdown` 에서 즉시
  `setPointerCapture` 를 걸면 이후 `click` 이 원래 대상이 아니라 캡처한 요소로
  전달된다. 임계값(4px)을 넘어 실제로 끌었을 때만 캡처하도록 고쳤다.
- **시간축이 마크와 어긋남.** 축이 창 전체 폭 기준, 마크는 거터 이후 기준이었다.
  축을 플롯과 같은 열에 넣어 좌표계를 통일했다.
- **구간 막대와 라벨이 서로를 덮음.** 미뤄뒀던 `collision.ts`(구간 분할)를
  구현해 레인별로 줄을 나눠 담는다.
- **화면을 덮는 시대 리본의 이름이 안 보임.** 시작점이 화면 밖이면 라벨을
  왼쪽 가장자리에 고정한다.

- 의존: Phase 1

### ✅ Phase 2R — UX 전면 재설계

Phase 2 는 기술적으로 성공했고(60fps) 제품적으로 실패했다. Gantt 차트였다.
뷰 계층을 전부 버리고 **시간 지형(Time Terrain)** 으로 다시 설계했다.
`engine/` 은 한 줄도 버리지 않았다 — ADR-003 의 경계 강제가 값을 했다.

진단한 아홉 가지의 해결:

| # | 문제 | 해결 |
|---|---|---|
| 1 | 화면의 70%가 빔 | 밀도 지형이 채운다 (실측 62%) |
| 2 | 레인 = 스프레드시트 행 | Y축을 연속 중요도로. 줌에 결합 |
| 3 | **줌이 종류를 안 바꿈** | cosmic→moment 5단 티어 변태 |
| 4 | 랜드마크 없음 | 수평선의 시간 해안선 8개 |
| 5 | 주변시 없음 | 항상 보이는 로그 수평선 + 현재 위치 띠 |
| 6 | 물리 없음 | 던지기 관성 · 줌 스프링 |
| 7 | 상세가 툴팁 | 목적지형 패널 + 뷰포트 전이 + 관계 칩 |
| 8 | 타이포 위계 없음 | 커서 리드아웃 64px ↔ 수평선 10px |
| 9 | **경외를 빈 화면으로** | "우주 전체의 0.00009% · 이 화면에서 0.001픽셀" |

부수적으로 해결한 것: 데이터 끝의 수직 절벽 → "지금" 마커 + 지형 페이드.

### ⬜ Phase 3 — 데이터 파이프라인 · **난이도 중상**

`scripts/etl/` — SPARQL → normalize → significance → chunk. `domains/history/` 생성.

실데이터 규모에서만 LOD 의 진짜 문제가 드러난다.

- 의존: Phase 1 타입 확정. `domains/history/seed.ts` 를 ETL 산출물로 교체한다
- 산출: `public/data/history/*.json` (커밋)
- 검증 리포트 필수: 날짜 파싱 실패율, significance 분포, 레인별 건수

### ⬜ Phase 4 — 탐색 UX · **난이도 중**

상세 패널, ⌘K 검색, URL 딥링크, 밀도 미니맵, 키보드 탐색.

1D 의 빈약함을 상쇄하는 요소들. **URL 딥링크는 나중에 붙일 수 없으므로 이 단계에서 반드시 넣는다.**

- 의존: Phase 2, 3

### ⬜ Phase 5 — 마감 · **난이도 중**

디자인 시스템 확정, 모션 폴리시, 접근성, SSG 이벤트 페이지, 배포.

포트폴리오 목표상 시각적 완성도가 곧 결과물이다.

### ⬜ Phase 6+ — MVP 이후

- **두 번째 도메인 추가 (AI 역사)** ← 엔진의 진짜 시험
- Canvas 렌더러 (성능이 실제로 부족할 때만)
- 관계 그래프 시각화
- 지도 레이어 (`d3-geo` + historical-basemaps)

> `engine/` 수정 0줄로 'AI 역사'를 얹을 수 있으면 이 프로젝트는 성공한 것이고, 아니면 "엔진"은 수사였을 뿐이다.

---

## 리스크 등록부

| 리스크 | 영향 | 상태 |
|---|---|---|
| `Date` 사용으로 범위 붕괴 | 치명 | ✅ 해소 — `TimePoint = number` + ESLint 차단 |
| 줌 배율 누적 정밀도 붕괴 | 높음 | ✅ 해소 — `{center, span}`, 200회 줌 테스트 |
| 심원한 시간에서 픽셀 양자화 | 중간 | ✅ 해소 — 정밀도 하한 (ADR-006) |
| 60fps 미달 | 높음 | ✅ 해소 — 브라우저 실측 p50 16.7ms, 드롭 0% (`verify:browser`) |
| 스코프 폭발 (8개 도메인 유혹) | 높음 | 🟡 완화 — MVP 는 history 1개, 경계는 ESLint 강제 |
| `significance` 서구 편향 | 중간 | 🟡 문서화 — DATA_MODEL.md 명시, 향후 다중 신호 |
| Wikidata 날짜 품질 불균일 | 중간 | ⬜ Phase 3 — `precision` 필드 + 검증 리포트 |
| 1D 탐색의 빈약함 | 중간 | ⬜ Phase 4 — 레인 + 미니맵 + 검색 + 딥링크 |
| 율리우스/그레고리력 | 낮음 | 🟡 수용 — proleptic Gregorian 고정 (ADR-005) |
