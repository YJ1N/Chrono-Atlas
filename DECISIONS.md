# DECISIONS.md

되돌리기 비용이 큰 결정만 기록한다. 나중에 바꿀 수 있는 것은 여기 적지 않는다.

각 항목: **결정 / 맥락 / 대안과 기각 사유 / 결과**

---

## ADR-001 — 시간의 코어 표현은 `number`(천문학적 연도)다

**상태:** 확정 · Phase 1 구현 완료

**맥락**
목표 범위는 138억 년이다. JavaScript `Date` 의 표현 한계는 ±8.64e15 ms ≈ **±271,821년**으로, 4자릿수 이상 모자란다. 이 선택은 나머지 모든 코드의 전제가 되므로 첫날에 못 박아야 했다.

**결정**
`type TimePoint = number` — 천문학적 연도 번호의 float64 소수. `0` = 기원전 1년(연도 0 문제를 표현 자체에서 흡수한다).

**대안과 기각 사유**

| 대안 | 기각 사유 |
|---|---|
| `Date` | 범위 부족. 논의 여지 없음 |
| `BigInt` 나노초 | 산술이 느려 60fps 뷰포트 갱신에 부적합. 심원한 시간에 나노초 정밀도는 무의미 |
| ISO 8601 문자열 | 파싱 비용, BCE 표기 비표준, 산술 불가 |
| `{ era, year, month, day }` 구조체 | 비교·보간·스케일 변환이 전부 함수 호출이 된다. 렌더 루프에서 치명적 |

**결과**
- float64 실측 해상도: 서기 2026년에서 약 14μs, 138억 년 전에서 약 96초. 정밀도가 필요한 구간에서 남고, 무의미한 구간에서만 성기다.
- `engine/` 안에서 `Date` 사용을 ESLint `no-restricted-globals` 로 금지했다. 표시 목적의 예외는 명시적 disable + 사유 주석으로만 허용한다.
- 검증: `src/engine/time/TimePoint.test.ts`

---

## ADR-002 — 뷰포트는 `{center, span}` 이고 d3-zoom 을 쓰지 않는다

**상태:** 확정 · Phase 1 구현 완료

**맥락**
138억 년 → 하루는 약 5e12배 줌이다. `d3-zoom` 은 화면 공간 변환 `{k, x, y}` 를 누적 관리하는데, `k` 가 1e12 규모에 이르면 `x` 오프셋 누적에서 float 정밀도가 무너져 패닝이 떨린다. 큰 동적 범위에서 알려진 실패 모드다.

**결정**
뷰포트를 `{ center: TimePoint, span: TimeDuration }` 으로 직접 소유한다. 줌은 `span *= factor`(커서 앵커 보정), 팬은 `center += delta`.

**대안과 기각 사유**
- `d3-zoom` + `scaleExtent([1, 5e12])` — 위 정밀도 문제. 또한 2D 변환을 1D 축에 억지로 맞추는 셈이라 코드가 오히려 늘어난다.
- `{start, end}` — 줌이 두 값의 동시 갱신이 되어 앵커 보정 식이 지저분해진다. `center/span` 이 줌·팬 대수를 각각 한 줄로 만든다.

**결과**
- 거대한 누적 스칼라가 존재하지 않아 전 구간 수치 안정. 200회 연속 줌인 후에도 유한하고 단조적임을 테스트로 확인했다.
- 트랙패드 핀치(macOS 에서 `ctrl+wheel` 로 도착)의 입력 정규화는 Phase 2 에서 자체 처리한다.
- 검증: `src/engine/time/TimeScale.test.ts`

---

## ADR-003 — `engine/` 은 `domains/` 를 알지 못한다 (ESLint 로 강제)

**상태:** 확정 · Phase 0 구현 완료

**맥락**
"Time Engine 이다"라는 주장은 검증되지 않으면 수사에 불과하다. 유일한 시험은 *도메인을 갈아끼울 때 엔진이 0줄 변경인가* 이다.

**결정**
의존 방향을 단방향으로 고정한다.

```
components/ ─▶ engine/
domains/    ─▶ engine/
engine/     ─▶ (없음)
```

`eslint.config.mjs` 의 `chronoatlas/engine-boundary` 규칙이 `src/engine/**` 에서 `domains`·`components`·`stores`·`app` 으로의 import 를 별칭·상대경로 양쪽 모두 차단한다.

**대안과 기각 사유**
- 문서로만 규정 — 사람의 규율에 맡기면 반드시 무너진다.
- 별도 npm 패키지로 분리 — 지금은 과잉. 경계만 지키면 나중에 언제든 추출 가능하다.

**결과**
- 위반 시 `npm run lint` 실패. 실제 위반 파일로 규칙 발동을 확인했다.
- 최종 시험은 Phase 6 에서 두 번째 도메인을 `engine/` 무수정으로 추가하는 것이다.

---

## ADR-004 — `Event` 와 `Era` 를 `TimelineItem` 하나로 통합한다

**상태:** 확정 · Phase 1 구현 완료

**맥락**
`CLAUDE.md` 의 데이터 모델 절은 `Event` 와 `Era` 를 별개 엔티티로 제시한다. 이 결정은 그에 대한 의도적 반론이다.

**결정**
단일 `TimelineItem` + `layer: 'context' | 'primary'`.

**근거**
두 엔티티의 데이터 *모양*이 동일하다 — 시간 구간, 제목, 중요도, 카테고리. 다른 것은 "어떻게 그리는가" 뿐이다. 분리하면 스케일·LOD·충돌회피·인덱스 코드가 전부 두 벌이 되며, 이는 `CLAUDE.md` 자신의 "Reuse everything / Avoid duplication" 과 충돌한다.

**결과**
- `IntervalIndex`, LOD, 충돌회피가 한 벌로 끝난다.
- 새로운 렌더 역할(예: 배경 그라디언트 밴드)이 필요하면 `ItemLayer` 에 값을 추가하는 것으로 끝난다.

---

## ADR-005 — 전 구간 proleptic Gregorian. 율리우스력 변환은 구현하지 않는다

**상태:** 확정

**맥락**
율리우스력→그레고리력 전환은 국가마다 다르다(교황령 1582, 영국 1752, 러시아 1918). 역사 데이터의 날짜가 어느 달력인지는 출처마다 다르고 명시되지 않는 경우가 많다.

**결정**
모든 `TimePoint` 를 proleptic Gregorian 으로 해석한다. 변환 엔진을 만들지 않는다.

**근거**
- 프로젝트 목표가 포트폴리오/기술 데모이므로 달력 고증은 핵심 가치가 아니다.
- 천문학적 연도 번호를 쓰면 윤년 규칙(`y%4==0 && (y%100!=0 || y%400==0)`)이 음수 연도에도 그대로 적용된다. 천문학이 이 표기를 쓰는 이유가 바로 이것이다.
- 일 단위 오차(최대 13일)는 세기 단위 이상으로 줌 아웃된 화면에서 1픽셀 미만이다.

**결과**
- 학술적 정확성을 요구하는 용도에는 부적합하며, 이 한계를 `DATA_MODEL.md` 에 명시한다.
- 필요해지면 `TimeSpan` 에 `calendar` 필드를 추가하는 것으로 확장 가능하다.

---

## ADR-006 — 정밀도 한계를 감추지 않고 줌 한계로 승격한다

**상태:** 확정 · Phase 1 구현 완료

**맥락**
Phase 1 테스트에서 실제로 발견된 문제다. 128억 년 지점에서 하루 폭까지 확대하면 `t - start` 상쇄 오차로 마크가 약 1.5px 단위로 튄다. float64 의 물리적 한계이며 알고리즘으로 제거할 수 없다.

**결정**
오차를 허용치로 흡수하는 대신, **중심점에 따라 최소 뷰포트 폭을 동적으로 제한**한다.

```
minResolvableSpan(center, width) = |center| × EPSILON × width / 0.25px
```

**근거**
128억 년 전에서 최소 폭은 약 6일이 된다. 그 시대에 일 단위 정밀도를 가진 데이터는 존재하지 않으므로 잃는 것이 없다. 역사 시대(`|center| < 1e5`)에서는 이 하한이 전역 최소값 아래라 아무 영향이 없다.

**결과**
- 양자화 오차가 전 구간에서 0.25px 미만으로 보장된다.
- 테스트가 "도달 가능한 뷰포트"만 검증하도록 바뀌었다 — 한계를 숨기는 대신 명시한다.

---

## ADR-007 — MVP 에서 TanStack Query 를 제외한다

**상태:** 확정

**맥락**
`CLAUDE.md` 의 선호 스택에 TanStack Query 가 있으나, 같은 문서가 "Keep APIs near zero / Prefer local processing" 도 요구한다.

**결정**
MVP 에서 제외한다. 정적 JSON 청크를 `dynamic import` + 모듈 캐시로 로드한다.

**근거**
TanStack Query 는 *서버 상태* 라이브러리다. 런타임 API 가 0개이면 해결할 문제 자체가 없다. 캐시 무효화·재검증·낙관적 갱신 중 어느 것도 정적 파일에 해당하지 않는다.

**결과**
- 런타임 fetch 소스가 2개 이상 생기면 재검토한다.
- 데이터 로딩 코드는 `engine/` 밖(`components/` 또는 전용 로더)에 두어 교체를 쉽게 한다.

---

## ADR-008 — d3 를 의존성으로 추가하지 않는다

**상태:** 확정 · Phase 1 구현 완료

**맥락**
`CLAUDE.md` 의 선호 스택에 D3.js 가 있다. 계획 단계에서는 "개별 모듈만 사용"으로 좁혔고, Phase 1 구현 결과 그마저 불필요했다.

**결정**
`d3-scale`·`d3-array`·`d3-interpolate`·`d3-format` 을 모두 추가하지 않는다.

**근거**
- `d3-scale` — 우리 스케일은 `{center, span}` 기반 선형 변환 두 줄이다.
- `d3-array` 의 `ticks()` — 1-2-5 사다리만 안다. 시간축은 그 아래에 달력이 있어 `1/12`(월)·`1/365`(일) 같은 비-10진 단위로 내려가야 하고, 라벨도 `"1969.4521"` 이 아니라 `"Jul 1969"` 여야 한다.
- `d3-interpolate` — 뷰포트 보간은 로그 보간 한 줄이다.
- `d3-format` — `Intl.NumberFormat` 이 내장이다.

**결과**
- `engine/time` 전체가 외부 의존성 0개다. 향후 npm 패키지 추출이 자유롭다.
- D3 가 필요해지는 지점은 지도 레이어의 `d3-geo` 이며, 그때 재검토한다.

---

## ADR-009 — 중요도는 Wikidata sitelink 수의 로그 정규화다

**상태:** 확정(설계) · Phase 3 구현 예정

**맥락**
LOD 가 이 제품의 심장이고, LOD 는 `significance` 없이는 동작하지 않는다. 이 값이 없으면 ChronoAtlas 는 스크롤 막대다.

**결정**
`significance = normalize(log(sitelinkCount))`. sitelink 수 = 해당 엔티티에 대한 문서가 존재하는 위키백과 언어판 개수.

**대안과 기각 사유**
- 수작업 큐레이션 — 수만 건에 확장 불가.
- Wikipedia pageviews — 최근성 편향이 크고 API 호출이 늘어난다.
- LLM 채점 — 비용, 재현 불가, 검증 불가.

**결과**
- 무료, ML 불필요, 빌드타임 1회 계산.
- **알려진 편향: 영어권·서구 중심.** 감출 수 없는 한계이며 `DATA_MODEL.md` 와 UI 각주에 명시한다. 향후 다중 신호로 보정한다.

---

## ADR-010 — 가상화의 형태는 LOD 다. Canvas 는 MVP 에서 만들지 않는다

**상태:** 확정(설계)

**맥락**
`CLAUDE.md` 는 "SVG first" 와 "Virtualization for large datasets" 를 동시에 요구한다. 대량 데이터에서 SVG 는 무너지므로 표면적으로 충돌한다.

**결정**
LOD 로 화면 내 DOM 노드 수에 **상한을 고정**한다(마크 ~300 + 축 ~40). 그러면 SVG 로 충분하다. `engine/render/Renderer.ts` 인터페이스만 미리 두고 Canvas 구현은 Phase 6 으로 미룬다.

**근거**
스크롤 윈도잉식 가상화는 세로 리스트의 해법이다. 시간축에서 올바른 대응물은 *중요도 기반 선별*이며, 이는 성능 대책인 동시에 UX 그 자체다 — 사람은 화면에 300개 넘는 마크를 인지하지 못한다.

**결과**
- 별도 가상화 라이브러리 불필요.
- Canvas 는 "성능이 실제로 부족할 때 여는 탈출구"이지 선제 구현 대상이 아니다.

---

## ADR-011 — 눈금 라벨의 단위는 시점 크기가 아니라 스텝 크기로 고른다

**상태:** 확정 · Phase 1 구현 완료

**맥락**
Phase 1 테스트에서 발견된 버그다. 68억 년 전을 확대하면 `"6.894 Ga"` 라벨이 열 번 반복됐다. 소수 자릿수를 늘려도 `"6.8934989 Ga"` 는 읽을 수 없다.

**결정**
라벨 단위를 `yearsAgo` 의 크기가 아니라 **눈금 스텝의 크기**로 결정한다. 스텝이 작아지면 Ga → Ma → 절대 연도(BCE/CE)로 자동으로 내려간다.

**부수 결정**
`ka` 단위는 눈금 라벨에서 쓰지 않는다. 20만 년 전을 `"200 ka"` 로 쓰면 읽기 좋지만 68억 년 전은 `"6,893,499 ka"` 가 되어 오히려 나빠진다. 그 구간은 절대 연도가 더 정직하다. (`formatTimePoint` 은 상세 패널용이므로 `ka` 를 유지한다 — 문맥이 다르다.)

**결과**
- "한 화면 안에서 major 라벨이 절대 중복되지 않는다"가 전 줌 범위 불변식으로 테스트된다.
- 알려진 한계: 뷰포트가 한 달보다 좁아지면 하루가 눈금의 최소 단위라 간격이 목표보다 넓어진다. 시(hour) 눈금은 만들지 않는다 — 어떤 데이터셋도 시 단위 정밀도를 갖지 않는다.

---

## ADR-012 — 줌은 배율이 아니라 표현을 바꾼다 (티어 변태)

**상태:** 확정 · Phase 2R 구현 완료

**맥락**
ADR-010 에서 "LOD 가 Maps 의 의미 수준 변화에 대응한다" 고 주장했지만, Phase 2 의 실제 구현은 **모든 줌에서 동일한 시각 언어(점+라벨)에 개수만 달랐다.** 138억 년에서도 점, 1년에서도 점이었다. 그래서 결과물이 Gantt 차트로 읽혔다.

Google Maps 는 z3 에서 국가 윤곽을, z18 에서 건물 발자국과 횡단보도를 보여준다. 같은 것을 크게 그리는 것이 아니라 **다른 것을 그린다.**

**결정**
폭에 따라 다섯 티어(`cosmic` · `epochal` · `historical` · `detail` · `moment`)를 정의하고, 티어마다 렌더 파라미터(지형 불투명도, 라벨 수, 마크 반경, 요약 노출)를 다르게 준다. 넓은 쪽에서는 지형이 주인공이고 개별 사건이 존재하지 않으며, 좁은 쪽에서는 지형이 사라지고 사건이 카드가 된다. **이 역전이 변태다.**

전이는 하드 스위치가 아니라 경계에서 두 티어를 섞는 크로스페이드다. `tierWeights(span)` 이 합이 1 인 가중치를 돌려주고, 렌더러는 그것을 레이어 `opacity` 에 그대로 꽂는다 — React 재렌더 없이 컴포지터에서 처리된다.

**결과**
- 양 끝 티어에도 **유한한** 경계를 준다. `Infinity` 를 쓰면 그 티어만 혼합 공식이 달라져 경계에서 불투명도가 0.084 튄다. 실제로 그렇게 만들었다가 테스트에 걸렸다.
- "전 줌 범위에서 지형 불투명도가 5% 이상 점프하지 않는다" 를 불변식으로 테스트한다.

---

## ADR-013 — Y축은 중요도이고, 줌에 결합된다

**상태:** 확정 · Phase 2R 구현 완료

**맥락**
Phase 2 는 레인(범주형 트랙)을 Y축이라 불렀지만 그것은 스프레드시트의 행 머리글이다. 고정 분류를 탐험 전에 강요하고, 세로로 이동할 수 없으며, 화면의 70%를 비게 만들었다.

**결정**
`Y = significance` (연속). 그리고 **세로 팬을 별도 제스처로 두지 않고 줌에 결합한다** — `visibleSignificanceRange(span) → [floor, 1]`. 넓게 보면 큰 것만, 좁혀 들어가면 사소한 것까지 떠오른다.

**근거**
Maps 에서 확대하면 카페가 *저절로* 나타난다. 사용자가 "덜 중요한 것도 보여줘" 라는 별도 조작을 하지 않는다. 결합하면 제스처가 하나로 줄고, 부수적으로 `Viewport` 타입이 `{center, span}` 그대로 유지되어 ADR-002 가 깨지지 않는다.

**결과**
- 하한 최댓값은 0.85 다. 1.0 이면 138억 년 뷰에서 화면이 빈다.
- 하한 근처에서 `fadeInOpacity` 로 서서히 떠오르게 한다. 딱 잘라 없애면 줌 중에 마크가 깜빡인다.
- **알려진 위험:** `significance` 의 서구 편향이 이제 제품의 *지형* 그 자체다. 문서 각주로 적는 것과 차원이 다르다. 향후 축을 다른 지표로 재바인딩할 수 있게 설계를 열어둔다.

---

## ADR-014 — 본문은 선형, 수평선만 로그

**상태:** 확정 · Phase 2R 구현 완료

**맥락**
138억 년을 선형으로 그리면 문자 이후 인류사 12,000년은 전체의 0.00009%, 1440px 화면에서 **0.001px** 이다. 즉 존재하지 않는다. Phase 2 가 "전체 보기" 에서 빈 화면을 보여준 이유가 정확히 이것이다.

Google Maps 는 메르카토르로 지구를 **왜곡해서** 쓸모를 만든다. 시간도 투영이 필요하다.

**결정**
본문 뷰포트는 **선형**으로 두고, 항상 보이는 하단 수평선 띠에만 로그 투영을 쓴다.

```
p(t) = 1 − ln(1 + (현재 − t)) / ln(1 + 전체 기간)
```

**근거**
로그 축 위에서는 두 사건의 간격을 눈으로 비교할 수 없다 — 측정이 거짓말이 된다. 본문은 재는 곳이고 수평선은 어디쯤인지 아는 곳이다. 역할이 다르므로 투영도 달라야 한다.

**결과**
- 최근 12,000년이 수평선의 30~60% 를 차지한다. 인류사가 비로소 보이는 크기가 된다.
- 각 자릿수가 비슷한 몫(5~15%)을 갖는 것을 테스트로 고정했다.
- 수평선은 뷰포트 **바깥을 어둡게** 해서 현재 위치를 보인다. 띠를 칠하면 전체 범위에서 화면이 균일해져 아무것도 구별되지 않는다.

---

## ADR-015 — overview 는 번들, detail 청크는 정적 파일 fetch

**상태:** 확정 · Phase 3 구현 완료

**맥락**
ADR-007 은 정적 청크를 "`dynamic import` + 모듈 캐시" 로 읽는다고 적었고, ROADMAP·DATA_MODEL 은 산출 위치를 `public/data/history/` 라고 적었다. **이 둘은 동시에 만족할 수 없다** — `public/` 은 번들러의 모듈 그래프에 없다. Phase 3 에서 실제로 구현하려 하자 충돌이 드러났다.

**결정**
역할에 따라 나눈다.

| 대상 | 위치 | 읽는 법 | 이유 |
|---|---|---|---|
| overview (상위 중요도 + 전체 `context`) | `src/domains/history/data/` | 정적 import | 첫 페인트에 지형이 이미 있어야 한다 |
| detail 청크 | `public/data/history/` | `fetch` + 모듈 스코프 캐시 | 대부분의 세션이 여기까지 확대하지 않는다 |

ADR-007 의 **원칙**(런타임 API 0개, 서버 상태 라이브러리 불필요)은 그대로다. 바뀐 것은 메커니즘 조항뿐이다. 정적 파일 요청은 API 가 아니며 브라우저·CDN 캐시를 그대로 탄다.

**대안과 기각 사유**

- **전부 `dynamic import`** — JSON 이 JS 모듈로 변환되어 페이로드가 커지고, 커밋된 산출물을 diff 로 검토하기 나빠진다.
- **전부 `fetch`** — 콜드 오픈 직후 지형이 빈 채로 한 프레임 이상 남는다. 이 제품에서 가장 나쁜 첫인상이다.

**결과**
- 청크 선택은 시간 겹침**만으로는 안 된다.** 138억 년 뷰에서는 모든 청크가 겹치므로 첫 화면에 전부 내려받게 된다 — 지연 로딩이 아니라 지연된 일괄 로딩이다. `ChunkManifest.significanceRange` 를 추가해, 줌이 정하는 중요도 하한(ADR-013)보다 청크의 상한이 낮으면 네트워크 이전에 건너뛴다.
- 청크 분할은 **시간이 아니라 항목 수**로 자르고 범위는 결과에서 읽는다. 시간으로 균등 분할하면 마지막 버킷에 99% 가 들어간다.
- 긴 `context` 항목(신생대 6600만 년 등)은 전부 overview 다. detail 청크에 들어가면 그 청크의 범위가 통째로 늘어나 범위 기반 로딩이 무의미해진다.

---

## ADR-016 — 카테고리는 사후 분류가 아니라 "어느 쿼리가 뽑았는가" 로 정한다

**상태:** 확정 · Phase 3 구현 완료

**맥락**
Wikidata 항목을 우리 카테고리 7개에 넣어야 한다. 통상적인 방법은 수집한 뒤 `P31`(instance of) 값을 보고 역추론하는 것이다.

**결정**
카테고리·레인·레이어를 **쿼리 정의에 붙인다.** "전투를 묻는 쿼리가 가져온 것은 전투다."

**대안과 기각 사유**
- `P31` 역매핑 표 — 수천 개의 클래스를 7개 통으로 보내는 표를 사람이 유지해야 하고, Wikidata 온톨로지가 바뀔 때마다 낡는다. 한 항목이 여러 클래스에 속하면 우선순위 규칙이 또 필요하다.
- LLM 분류 — 비용·재현 불가·검증 불가 (ADR-009 와 같은 이유).

**결과**
- 새 카테고리 추가 = `SOURCES` 배열에 항목 하나 추가.
- 한 항목이 두 쿼리에 잡히면 **먼저 선언된 소스가 이긴다.** 따라서 `SOURCES` 의 **배열 순서가 곧 카테고리 우선순위**이며, 그 순서에 의미가 있다는 사실이 코드에 드러나 있어야 한다.
- 대가: 커버리지가 쿼리 목록의 함수가 된다. 목록에 없는 종류의 사건은 존재하지 않는다. 이것은 숨겨진 결함이 아니라 **의도된 큐레이션 지점**이며, 리포트의 카테고리별 건수가 그 편중을 매번 드러낸다.

---

## ADR-017 — 딥링크 URL 의 숫자를 반올림하지 않는다

**상태:** 확정 · Phase 4 구현 완료

**맥락**
공유된 링크는 회수할 수 없다. 한 번 나간 URL 이 다른 화면을 열면 그건 영구적인 결함이다. 그래서 이 결정은 되돌리기 비용이 큰 쪽에 속한다.

처음에는 URL 을 짧게 하려고 좌표를 12 유효숫자로 줄이려 했다. 계산해 보니 틀렸다. 복원 오차를 픽셀로 환산하면

```
오차px = |center| × 10⁻ᵈ ÷ span × width
```

이고, 128억 년 전에서 최대로 확대한 지점(span ≈ 0.016년, ADR-006 의 정밀도 하한)에 d=12 를 넣으면 **약 1,100px** 이 나온다. 화면을 통째로 벗어난다. 0.25px 아래로 누르려면 d ≈ 16 이 필요한데, 그것은 float64 가 가진 전부다.

**결정**
`String(number)` 를 쓴다. JS 의 숫자→문자열은 **정확히 왕복하는 가장 짧은 표현**을 보장하므로 짧게 만들려는 목적과 정확성이 동시에 달성된다.

**결과**
- 도달 가능한 모든 뷰포트에서 왕복 오차가 **정확히 0px** 임을 테스트로 고정했다. 심원한 시간 최대 확대 지점을 포함한다.
- 반올림했다면 실패했을 것임을 보여주는 테스트를 같이 둔다. 나중에 누가 "URL 이 길다" 며 줄이려 할 때 근거가 코드 안에 있어야 한다.
- 대부분의 뷰포트에서는 어차피 `t=0&s=6000` 처럼 짧다. 길어지는 것은 극단적으로 확대했을 때뿐이며, 그때는 정확성이 길이보다 중요하다.
- **`replaceState` 를 쓴다.** 뷰포트는 연속적으로 바뀌므로 `pushState` 면 한 번 탐험에 히스토리가 수십 개 쌓여 뒤로 가기가 쓸모없어진다.

---

## ADR-018 — `Lane` 을 유지한다. 렌더링이 아니라 LOD 분산 축이다

**상태:** 확정 · Phase 4 재검토 완료

**맥락**
ADR-013 에서 Y축이 연속 중요도가 되면서 레인은 렌더링 역할을 잃었다. `DATA_MODEL.md` 는 "Phase 4 에서 쓰이지 않으면 제거를 검토한다" 고 적었고, Phase 4 에서 제거하려 했다.

**제거하려다 발견한 것**
`laneId` 는 죽은 필드가 아니었다. `engine/index/lod.ts` 의 점유 격자가 이 값을 키로 쓴다.

```ts
if (!occupancy.claim(item.laneId, x)) continue;
```

같은 x-빈이라도 레인이 다르면 둘 다 살아남는다. 즉 레인은 **같은 시각의 서로 다른 종류를 함께 보이게 하는 장치**다. 제거하면 같은 해의 전투와 과학 발견 중 하나만 살아남아, 선별 결과가 조용히 빈약해진다.

**결정**
유지한다. 다만 이름이 함의하는 "화면의 가로 트랙" 이 아니라 **LOD 분산 축**임을 문서에 명시한다.

**대안과 기각 사유**
- 제거하고 `categoryId` 로 점유 키를 대체 — 동작은 비슷하겠지만 카테고리는 7개, 레인은 5개로 분산 정도가 달라진다. 이유 없이 선별 동작을 바꾸는 변경이다.
- 필터 UI 로 승격 — 카테고리가 색 토큰을 이미 갖고 있어 필터에는 그쪽이 낫다. 필터 자체는 Phase 5 로 미룬다.

**결과**
- 이름과 실제 역할이 어긋나 있다는 것이 남은 부채다. 두 번째 도메인을 붙일 때(Phase 6) 이 값이 그 도메인에서도 자연스러운지가 진짜 시험이 된다.
